{
  "name": "imprenta-irlanda",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "test": "jest",
    "lint": "eslint . --ext .js",
    "deploy:netlify": "netlify deploy --prod --dir=.",
    "deploy:ghpages": "npx gh-pages -d ."
    "deploy:donweb": "node deploy/ftp-deploy.js",
    "start-server": "node server/save-config.js"
  },
  "devDependencies": {
    "eslint": "^8.0.0",
    "jest": "^29.0.0"
  }
}
